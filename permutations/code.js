// Returns the factorial of a given natural number
function factorial (var n) {
	if (n == 1) {
		return 1;
	} else {
		return n * factorial (n - 1);
	}
}

// Returns the number of permutations of characters from a given string
function permutations (var str) {
	// TODO: Figure out how to actually implement this moster
}
